# version: 1.0.0

$schema: http://json-schema.org/schema#

type: object
required:
  - atStop
properties:
  version:
    type: string
  atStop:
    description: True if the vehicle is at stop, otherwise False
    type: boolean
